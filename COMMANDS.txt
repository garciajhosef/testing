Create branch
$ git branch <branch-name>

Move to branch
$ git checkout <branch-name>

Create new branch and switch you to it
$ git checkout -b <branch-name>

Publish new branch
$ git push -u <remote> <branch-name>


changes changes
add all files
$ git add . 
$ git add file.x


$ git push

Create PR Pull Request
Solve PR


My local master branch
git pull  = git fetch + git merge



git status
git diff

git commit -am "message"    add and commit(just for modified files) 

update local master branch from Repo   git pull
move to my branch 
git merge master --> update my branch with latest changes
