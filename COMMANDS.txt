Create branch
$ git branch <branch-name>

Move to branch
$ git checkout <branch-name>

Create new branch and switch you to it
$ git checkout -b <branch-name>

Publish new branch
$ git push -u <remote> <branch-name>